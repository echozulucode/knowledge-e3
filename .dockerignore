# Build artifacts and dependencies
node_modules
dist
.pnpm-store
.pnpm-virtual-store

# Version control
.git
.gitignore
.gitattributes

# Development files
.env
.env.local
.env.*.local
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary and cache files
tmp
temp
.cache
.next
build-cache

# Testing
coverage
.nyc_output

# Project-specific
derisk
.claude
*.md
tests
vitest.config.ts

# Runtime data — never belongs in the build context. `data/` holds the SQLite
# index and the git-of-record mirror at runtime (see the /data volume in the
# Dockerfile); a populated working tree here can add tens of MB to every build.
data
**/data
server/data
